import { describe, it } from "vitest";

/** Placeholder for the still-stubbed world scaffolder. */
describe("world (stubbed)", () => {
  it.todo("scaffoldAdventure writes a minimal adventure and refuses overwrite");
});
